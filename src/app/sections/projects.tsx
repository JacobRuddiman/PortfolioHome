'use client';

import React from 'react';

export default function Projects() {
  return (
    <section className="snap-start w-screen h-screen flex-shrink-0 flex items-center justify-center bg-gray-200 text-center p-10">
      <div>
        <h1 className="text-4xl font-bold mb-8">Projects Carousel</h1>
        <p>Here will be a 3D projects carousel or showcase of your projects.</p>
        <p>For now, this is placeholder content until the carousel is implemented.</p>
        {/* You can replace this later with the 3D project carousel */}
      </div>
    </section>
  );
}
